---
interface Props {
  href: string;
  className?: string;
}

const { href, className = "text-link" } = Astro.props;
---
<a href={href} class={className} target="_blank" rel="noopener noreferrer">
  <slot />
</a>
