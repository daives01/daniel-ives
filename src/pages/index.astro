---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const projects = await getCollection("projects");
const recentProjects = projects.filter((project) => project.slug === "moon-lander");
---

<BaseLayout>
  <h1 class="text-4xl font-bold mb-6">Daniel Ives</h1>
  
  <p class="mb-6">
    I'm a software engineer, husband, and musician. I work at <a href="https://bamboorose.com">Bamboo Rose</a> building full stack applications. 
    I've been coding for several years and focus on shipping fast and building things that work.
  </p>
  
  {recentProjects.length > 0 && (
    <>
      <p class="mb-4">
        I occasionally build <a href="/projects">projects</a> in my free time. Here are some of my most recent ones:
      </p>
      
      <div class="mb-6">
        {recentProjects.map((project) => (
          <div class="mb-2">
            <a href={`/projects/${project.slug}`}>{project.data.title}</a>
          </div>
        ))}
      </div>
    </>
  )}
  
  <p class="mb-6">
    You can read more <a href="/about">about me</a> and check out the <a href="/tools">tools I like</a>. 
    You can <a href="https://x.com/couponnephew">find me online</a>, or even <a href="https://linkedin.com/in/daniel-benjamin-ives">LinkedIn</a> for some reason.
  </p>
  
  <p>
    Feel free to <a href="mailto:contact@daniel-ives.com">reach out</a>.
  </p>
</BaseLayout>
